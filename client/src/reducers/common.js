import {

} from '../constants/actionTypes';
  
const defaultState = {
	appName: 'reloj',
	token: null,
	viewChangeCounter: 0
};

export default (state = defaultState, action) => {
	switch (action.type) {
		default:
	return state;
	}
};